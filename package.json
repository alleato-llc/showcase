{
  "name": "showcase",
  "private": true,
  "version": "0.1.0",
  "description": "A themeable static portfolio/showcase engine: an Astro site generator plus a Go tool (CLI + Wails GUI) to edit project entries.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @showcase/site",
    "build": "npm run build -w @showcase/site",
    "preview": "npm run preview -w @showcase/site"
  }
}
